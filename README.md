# Repentance Creek Public Hall

Static website for the Repentance Creek community hall — events, pizza nights, gallery, and hall hire.

## Local preview

```bash
python3 -m http.server 8080
```

Open http://localhost:8080

## Deploy

Connected to Vercel. Push to `main` for automatic deployments, or run:

```bash
vercel --prod
```
